apiVersion: v1
kind: Service
metadata:
  name: clouddental-portal-svc
  namespace: clouddental
  labels:
    app: clouddental-portal
spec:
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: clouddental-portal
  type: ClusterIP
